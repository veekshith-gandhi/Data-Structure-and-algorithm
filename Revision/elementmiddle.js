/** @format */

solving(`8
5 6 4 3 5 7 7 8`);

function solving(input) {
  input = input.split("\n");
  let n = +input[0];
  let arr = input[1].split(" ").map(Number);
  let isFound;
  for (let i = 1; i < arr.length - 1; i++) {
   
    }
  } 
  if (isFound) {
    console.log(isFound);
  } else {
    console.log("-1");
  }
}
